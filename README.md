Oracle timestamp column JPA demo
================================

This is a demo project for discussing timstamp column mapping in JPA. It is shown that using `ZonedDateTime` and `Instant` types leads to ignoring partitioning in ORCALE DB. 
